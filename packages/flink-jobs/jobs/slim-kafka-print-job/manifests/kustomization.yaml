apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flink-deploy.yaml
  - flink-config.yaml

secretGenerator:
  - name: simple-kafka-print-job-sec
    namespace: flink-job
    envs:
      - secrets.env
    options:
      disableNameSuffixHash: true
